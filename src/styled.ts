import { Box, styled } from '@mui/material';

export const ToDoListWrapper = styled(Box)({
  margin: '70px auto 0 auto',
  width: '40vw',
  height: '70vh',
  borderRadius: '8px',
  padding: '40px',
});
